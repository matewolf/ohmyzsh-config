# Steps to setup dev environment
### 1. Install ohmyzsh
```bash
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
```

### 2. Copy repo`s .zshrc to `~/.yshrc`

### 3. Install missing dependencies

### 4. Install cursor cli
